import { createMuiTheme } from '@material-ui/core/styles';

const theme = createMuiTheme({
  palette: {
    background: {
      default: '#24292E',
    },

    primary: {
      main: '#C0C8D1',
    },
    secondary: {
      main: '#1E444F',
    },
  },
});
export default theme;
